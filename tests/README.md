# Test Suites Placeholder

This directory will contain the test suites (Playwright tests) that will be run against the mutated applications.
The mutation framework will use these tests to determine if a mutant is killed.
